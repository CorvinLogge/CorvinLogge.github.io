Using this coupon code generator you can generate codes with length of your choice. You can also define the alphabet, that is used for generating the codes. You will receive an id which you can use to access your codes.

Note: This is just an example so the generated codes are only saved for a day.